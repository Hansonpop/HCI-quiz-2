Short Explanation:
-The form now has inline validation to make sure users enter valid inputs
-The website now has responsive breakpoints so it can be view on phone
-The site now has breadcrumbs so the users now how deep in they are
